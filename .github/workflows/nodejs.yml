name: NodeJS with Webpack

on:
  push:
    branches: ['master']
  pull_request:
    branches: ['master']

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [21.x]

    steps:
      - uses: actions/checkout@v3

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}

      - name: Shop.App.Web Tests
        working-directory: ./shop.app.web
        run: |
          npm install
          npm run test

      - name: Shop.App.Orders Tests
        working-directory: ./shop.app.orders.api
        run: |
          npm install
          npm run test
